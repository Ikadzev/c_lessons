int x;

if (/* x */) {
    //code a
} else { //можно без else
    //code b
}
//code c

//code a
while(x) {
    //code a
}

do {
    //code a
} while (x);

for (x = 0; x <= N; x = x + 1) { // можно x += 1 и x++ (++x)
    printf("%d ", x);
}
/* x++; == ++x; */
/* y = x++;(y = x до изм.)  != y = ++x;(y = x после изм.)*/
/* z = x = y (== z = (x = (y))) */
 